<h1 style="text-transform: uppercase">Management system</h1>  <!--Ira-->

<br />
<hr />
<br />

<div class="row">
    <div class="col-md-6">
        @*Added Role based configuration for Manager and Administrator Only - Victor Reis 05/11/2023*@
        @if (User.IsInRole("Manager") || User.IsInRole("Administrator"))
        {
            <h3>Manage shoes</h3>
            <br>

            <form method="post" action="@Url.Action("Index", "Shoe")">
                <div class="d-grid col-3 justify-content-beginning">
                    <input class="btn btn-custom" type="submit" value="Go to the page">
                </div>
            </form>
            <br />

        }
    </div>
    <div class="col-md-6">

        <h3>Manage orders</h3>
        <br>
        <form method="post" action="@Url.Action("Orders", "Order")">
            <div class="d-grid col-3 justify-content-beginning">
                <input class="btn btn-custom" type="submit" value="Go to the page">
            </div>
        </form>
    </div>
</div>
<br />
<hr />
<br />
<div class="row">
    <div class="col-md-6">
        @*Added Role based configuration for Manager and Administrator Only - Victor Reis 05/11/2023*@
        @if (User.IsInRole("Manager") || User.IsInRole("Administrator"))
        {

            <h3>Manage users</h3>
            <br>

            <form method="post" action="@Url.Action("Index", "User")">
                <div class="d-grid col-3 justify-content-beginning">
                    <input class="btn btn-custom" type="submit" value="Go to the page">
                </div>
            </form>
        }
    </div>
    <div class="col-md-6">
        @*Added Role based configuration Administrator Only - Victor Reis 05/11/2023*@
        @if (User.IsInRole("Administrator"))
        {

            <h3>Reports</h3>
            <br>
            <form method="post" action="@Url.Action("Index", "Report")">
                <div class="d-grid col-3 justify-content-beginning">
                    <input class="btn btn-custom" type="submit" value="Go to the page">
                </div>
            </form>
        }
    </div>
</div>
<br />
<hr />
<br />
<div class="row">
    <div class="col-md-6"> @*Ira. 16/11*@
        @if ( User.IsInRole("Administrator"))
        {

            <h3>Manage roles</h3>
            <br>

            <form method="post" action="@Url.Action("Index", "Account")">
                <div class="d-grid col-3 justify-content-beginning">
                    <input class="btn btn-custom" type="submit" value="Go to the page">
                </div>
            </form>
        }
    </div>
    <div class="col-md-6">
       
    </div>
</div>
<br />
<hr />
<br />